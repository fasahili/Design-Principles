package src.Controller;

public class Staff extends User {
    public Staff(int id, String name, int age, int hourlyRate, int numHoursPerMonthCompleted) {
        super(id, name, age, hourlyRate, numHoursPerMonthCompleted);
    }
}
